"""{{PROJECT_NAME}} — a self-contained MCP server in the pure-core + thin-adapters
shape: a deterministic data plane (exact compute, persisted state, durable
exports) that the LLM narrates, exposed over dual stdio/HTTP transport."""

__version__ = "0.1.0"
